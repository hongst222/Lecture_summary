//누적합을 구하기 위해 반복이 시작되기 전 합산에 사용할 변수를
//0으로 초기화, 반복문 안에서 누적합산 수행 1~10

let x = 0;
let i = 1;

while ( i <= 10){
    x += i;
    console.log(`x=${x}, i = ${i}`);
    i++;
}
console.log(`1부터 10까지의 합: ${x}`);
